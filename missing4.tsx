                          <Button 
                            variant="destructive" 
                            size="sm" 
                            onClick={() => onDelete(selectedReport.id)} 
                            className="font-extrabold uppercase text-[10px] tracking-widest flex items-center gap-1.5"
                          >
                            <Trash2 className="w-3.5 h-3.5" /> EXCLUIR RDO
                          </Button>
                        )}
